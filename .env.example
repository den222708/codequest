# CodeQuest Frontend Environment Variables
VITE_API_BASE_URL=http://localhost:3001/api/v1

# DevTools bypass (production only) — MD5 hash of your secret key.
# Generate: node -e "import('disable-devtool').then(m=>console.log(m.default.md5('YOUR_SECRET')))"
# VITE_DEVTOOL_BYPASS_HASH=
